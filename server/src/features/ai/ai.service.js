// Cleared: previous scaffold referenced `models/ai-interaction.model.js` whose
// table mapping didn't match the real schema. Rebuild against the actual
// `ai_interactions` table (columns: user_id, question_id, interaction_type,
// prompt_summary, response_summary, status, provider).
