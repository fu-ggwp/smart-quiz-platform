// Cleared: previous scaffold assumed a single `exams` table. The real schema
// splits this into `exam_sessions`, `exam_questions`, and `exam_attempts`
// (plus shared `attempt_answers`). Rebuild against those actual tables.
