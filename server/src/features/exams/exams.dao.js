// Cleared: previous scaffold assumed a single `exams` table plus
// `exam-attempt.model.js`/`attempt-answer.model.js` whose table mappings
// were wrong (`exams`, `learner_answers`). Rebuild against the actual
// `exam_sessions`, `exam_questions`, `exam_attempts`, and `attempt_answers` tables.
